export class Product {
    // tslint:disable-next-line: max-line-length
    constructor(public id: string, public title: string, public description: string, public price: number, public imageUrl: string, public purchaseQuantity: number, public returnQuantity: number, public category: string) {}
}
